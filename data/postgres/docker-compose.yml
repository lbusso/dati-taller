version: '3.8'
services:
  postgres:
    image: postgres:11
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_USER: 'dev-dati'
      POSTGRES_PASSWORD: 'Gesti@nDATI-cdc'

    ports:
      - '5432:5432'
volumes:
  postgres_data: